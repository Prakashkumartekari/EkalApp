import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const AddItemStock = () => {
  return (
    <View>
      <Text>Add Item Stock</Text>
    </View>
  );
};

export default AddItemStock;

const styles = StyleSheet.create({});
